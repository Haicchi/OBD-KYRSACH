<Window x:Class="TourAgency.View.AddTourWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:helpers="clr-namespace:TourAgency.Helpers"
        MouseDown="Window_MouseDown"
        Title="Конструктор Туру" Height="1020" Width="650"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F1F8E9">
    <Window.Resources>
        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2E7D32"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        <Style x:Key="AddSmallButton" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Content" Value="+"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Background" Value="#81C784"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Border Background="White" Margin="20" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="25">
                    <TextBlock Text="СТВОРЕННЯ НОВОГО ТУРУ" FontSize="22" FontWeight="Bold" 
                               Foreground="#1B5E20" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <TextBlock Text="Назва туру" Style="{StaticResource HeaderLabel}"/>
                    <TextBox Text="{Binding TourName, UpdateSourceTrigger=PropertyChanged}" Height="35" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Опис" Style="{StaticResource HeaderLabel}"/>
                    <TextBox Text="{Binding Description}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Країна" Style="{StaticResource HeaderLabel}"/>
                            <DockPanel LastChildFill="True">
                                <Button DockPanel.Dock="Right" Style="{StaticResource AddSmallButton}" Command="{Binding OpenAddCountryCommand}"/>
                                <ComboBox ItemsSource="{Binding Countries}" SelectedItem="{Binding SelectedCountry}" DisplayMemberPath="Name" Height="30" VerticalContentAlignment="Center"/>
                            </DockPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Місто" Style="{StaticResource HeaderLabel}"/>
                            <DockPanel LastChildFill="True">
                                <Button DockPanel.Dock="Right" Style="{StaticResource AddSmallButton}" Command="{Binding OpenAddTownCommand}"/>
                                <ComboBox ItemsSource="{Binding FilteredTowns}" SelectedItem="{Binding SelectedTown}" DisplayMemberPath="Name" Height="30" VerticalContentAlignment="Center"/>
                            </DockPanel>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Готель" Style="{StaticResource HeaderLabel}"/>
                    <DockPanel LastChildFill="True">
                        <Button DockPanel.Dock="Right" Style="{StaticResource AddSmallButton}" Command="{Binding OpenAddHotelCommand}"/>
                        <ComboBox ItemsSource="{Binding FilteredHotels}" SelectedItem="{Binding SelectedHotel}" DisplayMemberPath="Name" Height="30" VerticalContentAlignment="Center"/>
                    </DockPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Дата вильоту" Style="{StaticResource HeaderLabel}"/>
                            <DatePicker SelectedDate="{Binding DepartureDate}" Height="30" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Дата повернення" Style="{StaticResource HeaderLabel}"/>
                            <DatePicker SelectedDate="{Binding ArrivalDate}" Height="30" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Тип харчування" Style="{StaticResource HeaderLabel}"/>
                            <DockPanel LastChildFill="True">
                                <Button DockPanel.Dock="Right" Style="{StaticResource AddSmallButton}" Command="{Binding OpenAddFoodCommand}"/>
                                <ComboBox ItemsSource="{Binding Foods}" SelectedItem="{Binding SelectedFood}" DisplayMemberPath="Type" Height="30" VerticalContentAlignment="Center"/>
                            </DockPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Всього місць" Style="{StaticResource HeaderLabel}"/>
                            <TextBox Text="{Binding TotalSeats, UpdateSourceTrigger=PropertyChanged}" 
                 Height="30" 
                 VerticalContentAlignment="Center">
                                <i:Interaction.Behaviors>
                                 
                                    <helpers:NumericInputBehavior AllowPlus="False"/>
                                </i:Interaction.Behaviors>
                            </TextBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Тип відпочинку" Style="{StaticResource HeaderLabel}"/>
                    <DockPanel LastChildFill="True">
                        <Button DockPanel.Dock="Right" Style="{StaticResource AddSmallButton}" Command="{Binding OpenAddTourTypeCommand}"/>
                        <ComboBox ItemsSource="{Binding TourTypes}" SelectedItem="{Binding SelectedTourType}" DisplayMemberPath="Type" Height="30" VerticalContentAlignment="Center"/>
                    </DockPanel>

                    <TextBlock Text="Фото туру (вертикальне)" Style="{StaticResource HeaderLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding PhotoPath}" IsReadOnly="True" Height="35" VerticalContentAlignment="Center" Background="#F5F5F5"/>
                        <Button Grid.Column="1" Content="Огляд..." Command="{Binding SelectPhotoCommand}" Style="{StaticResource ModernButtonStyle}" Background="#455A64" Height="35" Margin="5,0,0,0"/>
                    </Grid>

                    <Border Margin="0,15,0,0" Height="240" Width="180" HorizontalAlignment="Center" CornerRadius="10" Background="#E8F5E9" BorderBrush="#C8E6C9" BorderThickness="1">
                        <Image Source="{Binding PhotoPath}" Stretch="UniformToFill">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Style.Triggers>
                                        <Trigger Property="Source" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,10">
                        <Button Content="ЗБЕРЕГТИ ТУР" Command="{Binding SaveTourCommand}" Width="200" Height="45" Background="#4CAF50" Style="{StaticResource ModernButtonStyle}"/>
                        <Button Content="СКАСУВАТИ" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="120" Height="45" Background="#EF5350" Margin="15,0,0,0" Style="{StaticResource ModernButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>